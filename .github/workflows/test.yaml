on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Test The ArbiHunter Feature Action
        uses: TheArbiHunter/feature-action@latest
